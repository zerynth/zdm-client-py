

class ADMClient(object):
    """
    A client for communicating with a ADM server.
    Example:
        >>> import adm
        >>> client = adm.ADMClient(base_url='unix://var/run/docker.sock')
    
    """

    def __init__(self,  ):
        self